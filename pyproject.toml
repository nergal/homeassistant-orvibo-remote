[project]
name = "homeassistant-orvibo-remote"
version = "0.0.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "homeassistant>=2025.12.5"
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
